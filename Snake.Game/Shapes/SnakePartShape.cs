using System.Windows.Media;

namespace Snake.Game.Shapes
{
    public class SnakePartShape : ShapeBase
    {
        public SnakePartShape(int x, int y, int xSize, int ySize, Brush brush) : base(x, y, xSize, ySize, brush)
        {
        }
    }
}
